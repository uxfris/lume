{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @workspace/database db:generate && pnpm --filter @workspace/worker... build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @workspace/worker start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ALWAYS",
    "restartPolicyMaxRetries": 10
  }
}
